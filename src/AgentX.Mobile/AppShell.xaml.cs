namespace AgentX.Mobile;

/// <summary>
/// Root Shell that hosts the four tab pages.
/// Navigation routes are declared in <c>AppShell.xaml</c>.
/// </summary>
public sealed partial class AppShell : Shell
{
    public AppShell()
    {
        InitializeComponent();
    }
}
