## .NET
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
*.userprefs
.vs/
*.rsuser

## Build results
[Dd]ebug/
[Rr]elease/
x64/
x86/
ARM64/
[Bb]uild/
bld/

## Test & coverage run artifacts (regenerated by `dotnet test --collect`; the
## coverage gate reads these in-place — they are never committed). See AX-QA-009.
TestResults/
.cov-tmp/
*.trx
coverage.cobertura.xml

## NuGet
*.nupkg
**/[Pp]ackages/*
!**/[Pp]ackages/build/
*.nuget.props
*.nuget.targets
project.lock.json

## EF Core design-time scratch database (created by `dotnet ef` tooling via
## AgentXDbContextFactory; never the user's real data — safe to delete).
agentx.design.db
agentx.design.db-shm
agentx.design.db-wal

## MSIX
*.msix
*.appxupload
AppPackages/
BundleArtifacts/

## IDE
.idea/
*.swp
*~
.vscode/

## OS
Thumbs.db
.DS_Store
desktop.ini

## Worktrees
.worktrees/

## Logs
*.log
logs/

## Secrets
*.pfx
key.properties
appsettings.Development.json

## AI Models (bundled in installer, too large for git)
models/*.gguf

## Installer output (build artifact, too large for git)
installer-output/

## Publish output (build artifact, regenerable via `dotnet publish`).
## Cross-platform Playwright Node binaries pushed individual files past
## GitHub's 100 MB limit; build artifacts don't belong in source control.
publish/

## Node.js (browser extension)
node_modules/
browser-extension/dist/

## Claude / Loki development tooling
.claude/
.loki/

## gstack QA tooling (run artifacts: reports, screenshots, evidence, coverage)
.gstack/

## LocaleAudit JSON report (generated when the tool runs from repo root)
audit-report.json
